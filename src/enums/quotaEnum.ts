export enum CategoryTreeType {
  sysQuota = 'list',
  userQuota = 'idx_user',
  sysTemplate = 'template',
  userTemplate = 'templ_user',
  product = 'product',
}

export enum SourceTypeEnum {
  wind = 'wind',
  bloomberg = 'bloomberg',
  reuter = 'reuter',
  mysteel = 'mysteel',
  cofeed = 'cofeed',
  formula = 'formula',
  website = 'website',
  eia = 'eia',
  usda = 'usda',
  manual = 'manual',
}
